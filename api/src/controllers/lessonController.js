export const uploadLesson = async (req, res) => {
    // Save file (req.file), title, description, teacherId, ageGroup
};

export const getLessonByAgeGroup = async (req, res) => {
    // Use req.user or query param to filter by child age group
};
